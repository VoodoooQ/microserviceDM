spring.application.name=guaumiau

# Configuración PostgreSQL - Supabase (Session Pooler)
# Usar variables de entorno en producción
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://aws-1-us-east-2.pooler.supabase.com:5432/postgres}
spring.datasource.username=${DATABASE_USERNAME:postgres.mwgzoamzvvehbatfsran}
spring.datasource.password=${DATABASE_PASSWORD:levelUpGamer1425}
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuración de red
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.connection-timeout=30000

# Configuración JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=${SHOW_SQL:false}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Puerto del servidor (variable de entorno en producción)
server.port=${PORT:8080}

# CORS - Permitir acceso desde Android
spring.web.cors.allowed-origins=${CORS_ORIGINS:*}
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
